# Testing and evaluation requirements for the Product Pricer model
# These packages are needed for model evaluation and testing

# PyTorch and CUDA support (same as training)
torch==2.5.1+cu124
torchvision==0.20.1+cu124
torchaudio==2.5.1+cu124

# Hugging Face ecosystem for model loading and evaluation
transformers==4.48.3
accelerate==1.3.0
datasets==3.2.0
peft==0.14.0
trl==0.14.0
bitsandbytes==0.46.0

# Model evaluation and testing
torch.nn.functional

# Visualization and analysis
matplotlib

# Additional utilities
requests==2.32.3
tqdm

# Note: Install PyTorch with CUDA support using:
# pip install --upgrade torch==2.5.1+cu124 torchvision==0.20.1+cu124 torchaudio==2.5.1+cu124 --index-url https://download.pytorch.org/whl/cu124
