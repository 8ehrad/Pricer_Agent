# Training requirements for fine-tuning the Product Pricer model
# These packages are needed for model training and fine-tuning

# PyTorch and CUDA support
torch==2.5.1+cu124
torchvision==0.20.1+cu124
torchaudio==2.5.1+cu124

# Hugging Face ecosystem
transformers
accelerate==1.3.0
datasets==3.2.0
peft==0.14.0
trl==0.14.0
bitsandbytes

# Optimization and quantization
bitsandbytes

# Monitoring and logging
wandb

# Additional utilities
requests==2.32.3
tqdm

# Note: Install PyTorch with CUDA support using:
# pip install --upgrade torch==2.5.1+cu124 torchvision==0.20.1+cu124 torchaudio==2.5.1+cu124 --index-url https://download.pytorch.org/whl/cu124
